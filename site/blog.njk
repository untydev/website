---
layout: layouts/page.njk
title: Dariusz Sobczyk's personal website and blog.
---
<div class="max-w-3xl mx-auto px-8">
  <main>
    <div class="mx-auto grid gap-14 grid-cols-1">
      {%- for post in collections.posts -%}
        <div>
          <!-- Meta -->

          <p class="text-md">
            <span class="text-gray-600 dark:text-gray-400">
              {{ post.data.date | publishDate }} in
            </span>
            <span class="text-gray-600 dark:text-gray-400">
              {%- for tag in post.data.tags or tags -%}
                {%- if tag != "post" -%}
                  {{ tag }}
                {%- endif -%}
              {%- endfor -%}
            </span>
            <span class="text-gray-600 dark:text-gray-400">
              &nbsp;·&nbsp; {{ post | readingTime }}
            </span>
          </p>

          <!-- Title -->

          <a href="{{ post.url }}" class="mt-2 block">
            <p class="font-display text-2xl font-bold text-gray-900 dark:text-gray-50">
              {{ post.data.title }}
            </p>
            <p class="mt-2 text-lg text-gray-600 dark:text-gray-400">
              {{ post.data.description }}
            </p>
          </a>

        </div>
      {%- endfor -%}
    </div>
  </main>
</div>
